description: weigand controller
include: base.yaml
compatible: "wiegand"

properties:
    gpios:
      type: phandle-array
      required: true

    label:
      type: string
      description: Descriptive name of the key
